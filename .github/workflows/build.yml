# name: Build Backend V2 - Flutter TFLite

# on:
#   push:
#     branches: ["main"]
#   pull_request:
#     branches: ["main"]
#   workflow_dispatch:

# concurrency:
#   group: ${{ github.workflow }}-${{ github.ref }}
#   cancel-in-progress: true

# env:
#   REGISTRY: ghcr.io
#   IMAGE_NAME: gustian3051/backend-v2
#   CNN_MODE: on_device_flutter

# jobs:
#   docker-build:
#     name: Docker build and publish
#     runs-on: ubuntu-latest

#     permissions:
#       contents: read
#       packages: write

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       - name: Login to GHCR
#         if: github.event_name != 'pull_request'
#         uses: docker/login-action@v3
#         with:
#           registry: ${{ env.REGISTRY }}
#           username: ${{ github.actor }}
#           password: ${{ secrets.GITHUB_TOKEN }}

#       - name: Docker metadata
#         id: meta
#         uses: docker/metadata-action@v5
#         with:
#           images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
#           tags: |
#             type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}
#             type=sha,prefix=sha-

#       - name: Build image
#         uses: docker/build-push-action@v6
#         with:
#           context: .
#           file: ./Dockerfile
#           push: ${{ github.event_name != 'pull_request' }}
#           tags: ${{ steps.meta.outputs.tags }}
#           labels: |
#             org.opencontainers.image.title=backend-v2
#             org.opencontainers.image.description=CNN model runs on Flutter/TFLite and backend receives detections.
#             app.cnn.mode=${{ env.CNN_MODE }}
#           cache-from: type=gha
#           cache-to: type=gha,mode=max